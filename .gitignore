# =============================================================================
# HDX-RDLS Metadata Crawler — .gitignore
# =============================================================================
#
# Strategy: Include notebooks, docs, configs, CSVs, summaries, and reports.
#           Exclude bulk JSON output files (26k+ raw metadata, 13k+ records,
#           35k+ HEVL blocks, 12k+ integrated, 12k+ dist).
#

# ----- Python -----
__pycache__/
*.py[codz]
*$py.class
*.so
*.egg-info/
*.egg
.installed.cfg
MANIFEST
.venv/
venv/
env/
ENV/
env.bak/
venv.bak/
.envrc

# ----- Jupyter -----
.ipynb_checkpoints/

# ----- OS -----
.DS_Store
Thumbs.db

# ----- IDE -----
.vscode/
.idea/
.cursorignore
.cursorindexingignore

# ----- Build / packaging (Python) -----
build/
develop-eggs/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
.Python
pip-log.txt
pip-delete-this-directory.txt
.ruff_cache/
.mypy_cache/
.dmypy.json
dmypy.json
.pyre/
.pytype/
cython_debug/
.pdm-python
.pdm-build/
.pixi
__pypackages__/
.pybuilder/
target/
.pypirc

# ----- Test / coverage -----
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py.cover
.hypothesis/
.pytest_cache/
cover/

# ----- Other frameworks -----
.scrapy
.webassets-cache
celerybeat-schedule
celerybeat.pid
*.sage.py
instance/
local_settings.py
db.sqlite3
db.sqlite3-journal
*.log
*.mo
*.pot
docs/_build/
/site
profile_default/
ipython_config.py
.ropeproject
.spyderproject
.spyproject
.abstra/
marimo/_static/
marimo/_lsp/
__marimo__/

# =============================================================================
# PROJECT-SPECIFIC: Bulk JSON and large binary exclusions
# =============================================================================

# ----- Large archive files -----
*.zip
*.parquet

# ----- Raw HDX metadata (26,246 JSON files) -----
hdx_dataset_metadata_dump/dataset_metadata/

# ----- NB 06 base RDLS records (13,053 JSON files) -----
hdx_dataset_metadata_dump/rdls/records/

# ----- NB 09-11 extracted HEVL block JSON files (35,199 files) -----
# Keep the CSVs (extraction results), exclude individual JSON blocks
hdx_dataset_metadata_dump/rdls/extracted/*.json

# ----- NB 12 integrated records (12,551 JSON files) -----
# Keep index CSVs and JSONL, exclude individual JSON records
hdx_dataset_metadata_dump/rdls/integrated/*.json

# ----- NB 13 dist tier JSON files (9,784 + 2,767 = ~12,551 files) -----
# Keep manifests, README, and reports; exclude individual JSON records
hdx_dataset_metadata_dump/rdls/dist/high/*.json
hdx_dataset_metadata_dump/rdls/dist/medium/*.json
hdx_dataset_metadata_dump/rdls/dist/low/*.json
hdx_dataset_metadata_dump/rdls/dist/invalid/

# =============================================================================
# INCLUDED (not ignored) — for reference:
# =============================================================================
# notebook/*.ipynb              — 13 pipeline notebooks
# docs/*.md                     — documentation + STORY.md
# README.md, progress_update.md — project docs
# requirements.txt              — dependencies
#
# hdx_dataset_metadata_dump/
#   config/*.yaml, *.csv        — signal dictionary, tag mappings, overrides
#   derived/*.csv, *.json, *.txt — classification results + summaries
#   policy/*.csv, *.txt         — OSM exclusion lists
#   reference/*.md, *.csv       — mapping rules
#   analysis/*.csv, *.json, *.py — signal analysis results
#
#   rdls/schema/*.json          — RDLS v0.3 JSON Schema (1 file)
#   rdls/template/*.json        — RDLS record template (1 file)
#   rdls/example/*.json         — 4 hand-crafted reference records
#   rdls/index/*.jsonl          — record index
#   rdls/extracted/*.csv        — 10 extraction result CSVs
#   rdls/integrated/*.csv,*.jsonl — integration index + skipped list
#   rdls/reports/*.csv, *.md    — QA reports
#   rdls/dist/master_manifest.csv — complete inventory
#   rdls/dist/README.md         — distribution summary
#   rdls/dist/reports/*.csv,*.md — full QA report suite
# =============================================================================
